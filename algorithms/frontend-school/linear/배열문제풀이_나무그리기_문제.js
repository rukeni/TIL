/* 나무 그리기 */

/* user code */
function answer(height) {
	const maxRow = height * 2 - 1;
	for (let i = 0; i < height; i++) {
    const centerChar = (i + 1)
    for(let j = 0; j < maxRow; j++) {

    }
  }

	return str;
}

/* main code */
let input = [
	// TC: 1
	3,

	// TC: 2
	5,

	// TC: 3
	7,
];

for (let i = 0; i < input.length; i++) {
	console.log(`#${i + 1} ${answer(input[i])}`);
}
